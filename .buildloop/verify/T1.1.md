VERIFY::v1
TASK: T1.1          RESULT: PASS
TRIAGE: none

== FINDINGS ==
MEDIUM | process | .buildloop/intent/T1.1.md:6 | Stages ran out of order: BUILD completed before INTENT, SPEC and PLAN existed
  evidence: the artifact chain was reconstructed after the build. Recorded
    rather than back-dated. The work itself verifies clean; the process did not
    follow the loop, which is the finding.
  disposition: accepted, not remediated. Rewriting history would be worse.

LOW | coverage | .buildloop/claims/T1.1.md | AC5 (prints legibly) has no automated check
  evidence: a @media print block exists in app/globals.css but was never
    rendered to paper or PDF. Declared as GAP:print-untested in the claims.

LOW | coverage | lib/profile.ts | Content correctness cannot be checked automatically
  evidence: the data was transcribed from a live page by reading it. No check
    can confirm it still matches the source. Declared as GAP:transcription.

== PASSES ==
SELF_CHECK  | 4 of 4 matrix checks re-run independently, all PASS
EVALS       | not applicable, no agent configuration changed
FORMAL      | skipped, ORDERING_SURFACE is none (static page, no interleavings).
              Gate run anyway to confirm it is wired: exit 0, nothing to verify.
CROSS_MODEL | not run, no DOUBT_CMD configured in this repo

== CLAIMS_AUDIT ==
DELTA_MANIFEST | 8 of 8 entries verified against the filesystem, all present
KNOWN_GAPS     | 5 declared, all accurate, none understated
SPEC_COVERAGE  | R1-R4 met and checked; R5 met by construction but UNTESTED

== RE_VERIFY ==
CHECK:build    | pnpm build                   | PASS
CHECK:types    | pnpm typecheck               | PASS
CHECK:contrast | pnpm check:contrast          | PASS (11 enforced pairs)
CHECK:emdash   | grep -rn '—' app lib scripts | PASS
