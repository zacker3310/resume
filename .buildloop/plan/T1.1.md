# Plan: Profile site at dev/resume

**id:** T1.1  **spec:** .buildloop/spec/T1.1.md
**premises checked:** new repo, no upstream, nothing to go stale

## Dependencies
next 16, react 19, tailwind 4, typescript. Scaffolded with create-next-app.
No dependency added beyond that scaffold.

## File operations (in order)
1. CREATE `scripts/contrast.mjs` - measure pairs first, before committing to a ramp
2. CREATE `lib/profile.ts` - `Role`, `Company` types; `profile`, `experience`, `education`, `skills`
3. MODIFY `app/globals.css` - acker tokens in `@theme`, focus ring, print rules
4. MODIFY `app/layout.tsx` - fonts, metadata from profile
5. MODIFY `app/page.tsx` - `SectionLabel`, `RoleEntry`, `Page`
6. MODIFY `package.json` - contrast gate ahead of `next build`
7. CREATE `README.md`

## Verification
CHECK:build     | pnpm build           | exit 0, static route
CHECK:types     | pnpm typecheck       | no errors
CHECK:contrast  | pnpm check:contrast  | all enforced pairs pass
CHECK:emdash    | grep -rn '—' app lib scripts | no matches

## Risks
- The acker accent may fail AA as text. Measure before using it. (It did fail.)
- Transcription drift between the live profile and the data module.

## Departures
Order was followed within BUILD, but BUILD ran before this plan existed.
Recorded in the verify report as finding F1.
