CLAIMS::v2
TASK: T1.1          PLAN: .buildloop/plan/T1.1.md

== DELTA_MANIFEST ==
FILES_CREATED: 4
  lib/profile.ts       | CREATE | profile, experience, education, skills, Role, Company | 314 lines, transcribed content
  scripts/contrast.mjs | CREATE | ENFORCED, DECORATIVE, ratio(), luminance()             | build gate, exits 1 on failure
  README.md            | CREATE | -                                                      | commands, contrast deviations
  TASKS.md             | CREATE | -                                                      | task queue
FILES_MODIFIED: 4
  app/globals.css | acker tokens in @theme, doubled focus ring, print block | -
  app/layout.tsx  | Geist + DM Sans, metadata derived from profile          | RootLayout
  app/page.tsx    | full page rewrite                                       | SectionLabel, RoleEntry, Page
  package.json    | contrast gate ahead of next build                       | build, typecheck, check:contrast

== SPEC_COVERAGE ==
CRITERION:R1 content from live profile | read via Claude in Chrome, 15 roles across 7 employers | manual
CRITERION:R2 single data module        | page imports lib/profile.ts, restates nothing          | CHECK:types
CRITERION:R3 acker tokens only         | one @theme block, no second palette, no blue           | CHECK:contrast
CRITERION:R4 static prerender          | route / marked static in build output                  | CHECK:build
CRITERION:R5 prints legibly            | @media print block                                     | UNTESTED

== BUG_FIXES ==
FIX:accent-as-text     | app/globals.css | red text uses --accent-text #E8695C | was: --accent #C0392B at 3.20:1, fails AA
FIX:button-on-accent   | scripts/contrast.mjs | button label is --text-primary   | was: --bg-base on accent at 3.39:1, fails AA
FIX:focus-ring-doubled | app/globals.css | inner --base ring then accent        | was: single ring at 2.85:1 on a raised card

== KNOWN_GAPS ==
GAP:print-untested   | The print stylesheet was never rendered to paper or PDF     | AC5 has no automated check
GAP:no-about-section | The page has no About section                              | the LinkedIn profile does not have one
GAP:not-deployed     | No remote, no deploy target                                | out of scope, intent left it open
GAP:transcription    | Content correctness rests on my reading of the live page   | no automated check possible
GAP:responsive-spot  | Only checked at desktop width in the browser               | grid collapses at sm by construction

== ORDERING_SURFACE ==
SURFACE:none
  Static prerendered page. No concurrency, no shared mutable state, no
  persistence ordering, no messaging, no partial failure. Nothing for a model
  checker to explore.

== VERIFICATION_MATRIX ==
CHECK:build    | pnpm build                   | exit 0, static route | PASS
CHECK:types    | pnpm typecheck               | no errors            | PASS
CHECK:contrast | pnpm check:contrast          | 11 pairs pass        | PASS
CHECK:emdash   | grep -rn '—' app lib scripts | no matches           | PASS
